<!-- STATUS -->
Epic: Prototype Harness
Feature: BoxStack 2D App-in-App Prototype
Task: B046 remove central feedback toast
<!-- /STATUS -->

# Active Session State

This file is the source of truth for the current prototype status. Keep it short and update it whenever the playable loop, next action, risks, or playtest verdict changes.

## Project Mode

Casual game, prototype-first. Skip heavy GDD/ADR/review workflow unless explicitly requested.

## Current Prototype

- Name: BoxStack playable prototype
- State: Core 2D stacking system validated as good enough for the next prototype phase.
- Goal: Turn the validated timing-based stacking loop into a Stack-like 2D vertical slice with abstract blocks, smooth color progression, minimal HUD, and mobile-readable physics.
- Current implementation: `Assets/Scripts/Prototype/BoxStackPrototype.cs` auto-bootstraps an orthographic camera, 2D floor, `Rigidbody2D`/`BoxCollider2D` stacking, input, win/fail state, stage-clear next-stage flow, `BoxStackStageProgressStore`-wrapped PlayerPrefs highest-stage unlock persistence, `BoxStackPrototypeAssetLoader`-wrapped config/font/sprite loading, `BoxStackPrototypeBoxVisualCatalog`-wrapped stage box-code to sprite/placeholder/floor/background visual selection, `BoxStackPrototypeUiStateFactory`-wrapped UI state/copy/stage-button state creation, Editor/development-build-only progress test controls (`진행 초기화`, `전체 해금`), safe-area-aware runtime UI state/events, screen-clamped centered constant-speed pre-drop horizontal movement that preserves stage speed even when the visible range is clamped, a one-use HUD undo button that restores the latest pre-drop snapshot during active play/drop/clear validation, high-friction parcel and floor contact materials (`friction = 8.0`, `bounciness = 0`), softened falling-box impact (`dropping gravityScale = 1.1`, fall-speed cap `4.5`, settled gravityScale = `1.6`), stronger damping (`linearDamping = 0.6`, `angularDamping = 0.8`), and rotation-locked vertical camera follow in `SampleScene`. B014 introduces `BoxStackPrototypeConfig` as the data boundary for stage/tuning values; `Assets/Resources/Prototype/BoxStackPrototypeConfig.asset` now stores the approved B013/B014 values and runtime loading is confirmed in Editor Play and rebuilt WebGL. If the asset is missing later, code fallback still preserves the same defaults. Clear requires boxes to stay in a single vertical column, using the first placed box as the center-x reference; any box that settles outside tolerance fails immediately, including delayed gravity collapse while playing or resolving a drop. After the stage target count is stacked, the stack must survive a 5-second validation window before success. The camera minimum y is derived from the floor bottom plus orthographic size with a small bottom padding, so the view does not drift below the conveyor/floor baseline. Imported parcel PNGs are scaled and collided by visible alpha bounds so transparent padding does not create floating gaps.
- B015 UI implementation: `Assets/Scripts/Prototype/BoxStackPrototypeUi.cs` now owns the Toss Minimal HUD, HUD undo button, stage select overlay, result popup, safe-area placement, runtime UI theme, and Korean font assignment through UI Toolkit. `BoxStackPrototype.cs` now feeds UI state/events into that layer instead of using Unity's `OnGUI` callback, and the unused legacy IMGUI drawing path has been removed. Editor Play confirmed `UIDocument` creation, `B015` HUD text, stage select overlay sizing, result popup sizing, and overlay pointer blocking. Unity still emits one known `PanelSettings` theme warning at runtime-created panel initialization, but the theme resource is loaded and UI renders correctly.
- Asset pipeline: Prototype art brief exists at `design/assets/boxstack-prototype-art-brief.md`; first AI-generated parcel PNG set is still preserved in `Assets/Art/Prototype/Parcel/`, and a logistics center background is still preserved in `Assets/Art/Prototype/Backgrounds/logistics_center_bg_01.png`. B046's active runtime direction bypasses those delivery assets and uses a code-generated edged abstract block with the accepted high-contrast internal grayscale gradient, no contact shadow, stage-palette floor, and a full-screen Stack-like light-bottom/dark-top rear background gradient sprite for faster visual iteration.
- UI exploration: Four HUD mockup variants were generated in `design/ui/hud-variants/`; variant 01 Toss Minimal was applied through a runtime UI Toolkit layer for playtest. On 2026-05-13, the current UI was judged too app-like for motivation, so a research pass and A/B/C mockup sheet were added at `design/ui/boxstack-game-ui-research-2026-05-13.md` and `design/ui/mockups/boxstack-ui-directions-2026-05-13.png`. Direction A, Delivery Arcade, is now implemented as the B016 first pass, with B026 borrowing the B/C mockup idea for a top-center box progress panel. B027 adds game-oriented free fonts: DNF BitBit v2 for HUD/title/button text, Gmarket Sans Bold for supporting text, and Noto Sans KR as fallback. B028 makes UI Toolkit apply those fonts through both `unityFont` and `unityFontDefinition`. B046 continues the Stack-like 2D visual direction based on Ketchapp `Stack`: color blocks with darker outer edges, high-contrast internal grayscale face gradient, no contact shadow after the rejected B042/B043 shadow tests, stack-height tint progression without landing color shift, softened full-screen light-bottom/dark-top stage-palette generated rear background gradient, minimal translucent HUD, and no central landing feedback toast. Result popup restart, stage select, and the HUD undo button are currently handled by `BoxStackPrototypeUi` instead of the previous runtime `OnGUI` drawing path. A tiny `B046` prototype build marker is shown below the top-right HUD area so mobile WebGL rebuild/caching can be checked quickly. `design/ui/delivery-arcade-assets/` remains a sidecar reference and is not wired into the current Stack-like runtime UI.
- Delivery Arcade implementation plan: User approved implementing the first Delivery Arcade HUD pass after recording this plan. Work should focus on `Assets/Scripts/Prototype/BoxStackPrototypeUi.cs`, with only minimal `Assets/Scripts/Prototype/BoxStackPrototype.cs` changes for build marker `B016` and optional UI state needed for simple landing feedback. Replace the full-width Toss Minimal top bar with a top-left shipping-label stage badge and top-right undo ticket; add a right-side delivery progress rail; add a central, simple state-driven feedback toast using Korean copy; restyle the result popup as delivery complete/failure cards. Preserve gameplay physics, stage difficulty, stage select behavior, undo behavior, safe-area handling, and overlay input blocking. Do not add scoring, combo, stars, reward-ad rescue, or gameplay tuning in this pass.
- B016 Delivery Arcade HUD pass: `BoxStackPrototypeUi` now replaces the full-width top bar with a compact shipping-label stage badge, top-right undo ticket, right-side delivery progress rail, central Korean feedback toast, and delivery-styled result card. `BoxStackPrototype` only increments the build marker to `B016`, changes the HUD stage label to Korean, and supplies simple state-driven feedback copy. `dotnet build BoxStack.slnx` passed with 0 warnings/errors. Unity CLI Connector on port `8090` confirmed Editor Play starts, one `UIDocument` exists, and the UI tree contains `B016`, the delivery-label stage badge, placed/target count, undo ticket, and stage select text. User accepted the overall UI layout and committed the pass as `7952526 B016 Delivery Arcade HUD 구조 적용`.
- B017 HUD undo cleanup: The code cleanup removed old fail-popup rescue and mock reward-ad runtime paths from `BoxStackPrototype`, kept the separate one-use HUD undo behavior, and bumped the build marker to `B017`. `dotnet build BoxStack.slnx` passed with 0 warnings/errors. Unity Editor Play Mode confirmed one `UIDocument`, build marker `B017`, undo snapshot restoration once per stage, and failure result text without ad/reward copy. Console still showed only the known `PanelSettings` theme warning and unrelated UDP warning. User accepted the cleanup and committed it as `af6cc91 B017 되돌리기 코드 정리`.
- B018 undo config naming cleanup: `BoxStackPrototypeConfig` now uses `UndosPerStage` instead of the old rescue/ad terminology. The checked-in config asset is migrated to `UndosPerStage: 1`, `BoxStackPrototype` reads the new field, and `FormerlySerializedAs("FreeRescuesPerStage")` preserves older config assets if they are encountered later. Gameplay tuning is unchanged. `dotnet build BoxStack.slnx` passed with 0 warnings/errors. Unity CLI Connector Editor Play verification confirmed build marker `B018`, config load, `configUndosPerStage = 1`, `runtimeUndosRemaining = 1`, and no remaining `FreeRescuesPerStage` or `AdRescuesPerStage` fields in `TuningSettings`. User committed it as `9d6ea93 B018 되돌리기 설정명 정리`.
- B019 UI Toolkit PanelSettings cleanup: `Assets/Resources/Prototype/Ui/BoxStackPanelSettings.asset` is now checked in with `BoxStackRuntimeTheme`, and `BoxStackPrototypeUi` loads that PanelSettings asset instead of creating one at runtime. Editor-only `AssetDatabase` fallback is used only to make Editor Play checks robust before Resources refresh timing catches up. `dotnet build BoxStack.slnx` passed with 0 warnings/errors. Unity CLI Connector Editor Play verification confirmed build marker `B019`, one `UIDocument`, resource PanelSettings loaded and used, theme assigned, and the previous `No Theme Style Sheet set to PanelSettings` warning removed. The unrelated Visual Studio UDP warning remains. User accepted the cleanup and committed it as `917f2b4 B019 UI PanelSettings 경고 정리`.
- B019 representative Editor Play structure check: Unity CLI Connector confirmed Play Mode starts and stops cleanly on port `8090`. Stages 1, 5, 10, 15, and 20 were loaded through reflection without changing gameplay code. All checked stages spawned the active box at center x `0.0`, showed build marker `B019`, and kept the screen-clamped movement range within the current camera view. Later stages correctly preserve difficulty intent with range compensation as the clamped visible range stays about `2.042` while the intended stage range grows from `2.328` at stage 5 to `3.185` at stage 20. Console showed only the known unrelated Visual Studio UDP warning and AIT/connector logs.
- B020 stage select unlock hit-test fix: User reported that the "unlock all" control appeared not to work and stages 10/15 did not respond. `BoxStackPrototypeUi` now makes the stage grid, progress-control row, and stage-button rows participate in UI Toolkit picking, and hidden overlays now use `display: none` plus hidden visibility so the inactive result popup cannot intercept clicks. `BoxStackPrototype` build marker is now `B020`. `dotnet build BoxStack.slnx` passed with 0 warnings/errors. Unity CLI Connector Editor Play verification confirmed `B020`, reset state starts with only stage 1 unlocked, unlock-all changes highest unlocked stage to 20, stages 10/15/20 become enabled, their centers are picked as `Button`, and selecting them changes the current stage to 10/15/20. User manually confirmed the fix and committed it as `b2d99b8 B020 스테이지 선택 해금 버튼 수정`. Console still shows only the known unrelated Visual Studio UDP warning and AIT/connector logs.
- B020 representative Editor Play structure check: Unity CLI Connector on port `8090` confirmed stages 1, 5, 10, 15, and 20 load after unlock-all, keep the active box at center x `0.0`, show build marker `B020`, keep one `UIDocument`, expose the Delivery Arcade HUD/stage-select text/buttons, preserve one undo, and keep the clamped move range inside the camera view. Stage 20 still uses the intended higher speed (`2.59`) while the visible move range remains clamped to about `2.042`. Console showed only the known unrelated Visual Studio UDP warning and AIT/connector logs.
- B020 manual playtest verdict: User judged the current state acceptable to proceed without more tuning. Keep the current Delivery Arcade HUD, B008 physics baseline, B012 screen-clamped movement, one-use HUD undo, and stage difficulty curve for the next checkpoint.
- B021 stage progress storage boundary: `BoxStackStageProgressStore` now wraps the prototype's highest-unlocked-stage PlayerPrefs access so App-in-Toss storage can be swapped later without changing gameplay flow. `BoxStackPrototype` now delegates stage progress load/save to that store and the build marker is `B021`. `dotnet build BoxStack.slnx` passed with 0 warnings/errors after Unity refreshed the new file. Unity CLI Connector Editor Play verification confirmed store type `BoxStackStageProgressStore`, reset progress saves stage 1, unlock-all saves stage 20, selecting stage 15 still works, one `UIDocument` exists, and `B021` appears. Console still shows only the known unrelated Visual Studio UDP warning and AIT/connector logs.
- B022 prototype asset loading boundary: `BoxStackPrototypeAssetLoader` now owns prototype config, Korean font, parcel/background sprite loading, Editor-only PNG fallback, runtime sprite creation, placeholder parcel creation, solid sprite creation, and visible-alpha rect calculation. `BoxStackPrototype` now delegates those loading details to the loader and the build marker is `B022`; gameplay, UI layout, physics, stage tuning, undo, and persistence behavior are unchanged. `dotnet build BoxStack.slnx` passed with 0 warnings/errors after Unity refreshed the new file. Unity CLI Connector Editor Play verification confirmed loader type `BoxStackPrototypeAssetLoader`, config loaded, Korean font `NotoSansKR-VF` loaded, floor sprite `parcel_stack_base_01` loaded, 3 box visuals loaded, one `UIDocument` exists, and UI labels include `B022`. Console still shows only the known unrelated Visual Studio UDP warning and AIT/connector logs.
- Delivery Arcade UI resource checkpoint: `cd85880 Delivery Arcade UI 리소스 초안 추가` committed `design/ui/delivery-arcade-assets/` with stage badge, undo ticket, progress rail/nodes, feedback stamp, and result card PNG drafts plus a README. These are sidecar design assets for review and future UI Toolkit `backgroundImage` skinning; the current runtime UI remains the B016/B024 vector-like Delivery Arcade implementation.
- B023 box visual catalog boundary: `BoxStackPrototypeBoxVisualCatalog` now owns parcel box asset definitions, stage box-code to visual selection, placeholder visual creation, placeholder sprite checks, floor sprite selection, and optional background sprite selection. `BoxStackPrototype` now asks the catalog for a visual and applies physics/gameplay behavior as before. Runtime marker is `B023`. `dotnet build BoxStack.slnx` passed with 0 warnings/errors after Unity refreshed the new file. Unity CLI Connector Editor Play verification on port `8093` confirmed catalog type `BoxStackPrototypeBoxVisualCatalog`, floor sprite `parcel_stack_base_01`, one `UIDocument`, and UI label marker `B023`. Console showed only known AIT logs and the unrelated Visual Studio UDP warning.
- B024 UI state factory boundary: `BoxStackPrototypeUiStateFactory` now owns runtime UI state creation, HUD/status/feedback/result copy, progress ratio, and stage-button state construction. `BoxStackPrototypeState` exposes the prototype flow state to that factory, while gameplay, physics, stage progression, undo, UI layout, and input behavior remain unchanged. Runtime marker is `B024`. `dotnet build BoxStack.slnx` passed with 0 warnings/errors. Unity CLI Connector on port `8093` confirmed Editor Play starts, one `UIDocument` exists, `_uiStateFactory` is `BoxStackPrototypeUiStateFactory`, and the UI marker shows `B024`. Console showed only known AIT logs and the unrelated Visual Studio UDP warning.
- B025 progress rail status text cleanup: `BoxStackPrototypeUi` no longer creates or updates the right-side progress rail's bottom state text label, and `BoxStackPrototypeUiStateFactory` no longer creates the unused HUD status copy. Progress nodes/fill, central feedback, gameplay, stage progression, undo, and result behavior are unchanged. Runtime marker is `B025`. `dotnet build BoxStack.slnx` passed with 0 warnings/errors; Unity CLI Connector Editor Play verification on port `8090` confirmed one `UIDocument`, marker `B025`, and no `_statusLabel` field.
- B026 top box progress panel: `BoxStackPrototypeUi` now replaces the right-side progress rail and numeric count text with a top-center horizontal progress panel. Placed boxes render as filled square slots, remaining boxes render as outline-only square slots, and gameplay, undo, stage select, result popup, and UI state factory behavior are unchanged. Runtime marker is `B026`. `dotnet build BoxStack.slnx` passed with 0 warnings/errors; Unity CLI Connector Editor Play verification on port `8090` confirmed one `UIDocument`, marker `B026`, no `_countLabel`/`_progressFill` fields, Row progress layout, and 4 progress slots on stage 1.
- B027 game font pass: `Assets/Resources/Prototype/Fonts/` now includes DNF BitBit v2 and Gmarket Sans Bold alongside the existing Noto Sans KR fallback plus a third-party font notice. `BoxStackPrototypeAssetLoader` loads display/body/fallback fonts separately, and `BoxStackPrototypeUi` applies the display font to HUD/title/button text and the body font to supporting text. Runtime marker is `B027`; gameplay, stage select, undo, physics, and progress UI behavior are unchanged. `dotnet build BoxStack.slnx` passed with 0 warnings/errors. Unity CLI Connector Editor Play verification on port `8090` confirmed all three fonts load through `Resources`, the UI holds `DNFBitBitv2`/`GmarketSansBold`/`NotoSansKR-VF`, one `UIDocument` exists, the UI marker shows `B027`, and the console has 0 errors/warnings.
- B028 UI Toolkit font definition fix: `BoxStackPrototypeUi.ApplyText` now assigns both `unityFont` and `unityFontDefinition` from the selected font so UI Toolkit's text renderer has an explicit font definition. Runtime marker is `B028`; gameplay and UI layout are unchanged. `dotnet build BoxStack.slnx` passed with 0 warnings/errors. Unity CLI Connector Editor Play verification on port `8090` confirmed one `UIDocument`, `B028`, and matching inline/resolved `unityFont` plus inline/resolved `unityFontDefinition` for DNF BitBit v2 and Gmarket Sans Bold text elements; console errors/warnings are 0.
- B029 Stack-like 2D visual redesign first pass: `design/ui/boxstack-stack-like-2d-design-guide-2026-05-15.md` records the new direction. `BoxStackPrototype` enables the abstract visual mode and runtime marker `B029`; `BoxStackPrototypeAssetLoader` creates abstract block, floor, and gradient background sprites at runtime; `BoxStackPrototypeBoxVisualCatalog` selects those generated visuals instead of delivery PNGs; `BoxStackPrototypeUi` and `BoxStackPrototypeUiStateFactory` remove delivery-themed colors and copy from the active HUD/result path. `dotnet build BoxStack.slnx` passed with 0 warnings/errors. Editor Play visual confirmation is still pending.
- B030 Stack-like 2D stage palette pass: `BoxStackPrototypePalette` now defines six same-family palette families and `BoxStackPrototype` applies the current stage palette to generated background/floor sprites and block tints. `BoxStackPrototypeUiStateFactory` passes that palette into `BoxStackPrototypeUi`, where undo/stage buttons, progress slots, feedback tint, panels, and result stamp color follow the same stage palette. `dotnet build BoxStack.slnx` passed with 0 warnings/errors. Unity CLI Connector Editor Play structure check confirmed stages 1, 10, and 20 show marker `B030`, distinct palette values, generated `16x256` backgrounds, generated `256x32` floors, expected progress slot counts, and 0 console warnings/errors. Manual Game view visual judgement is still pending.
- B031 Stack-like 2D gradient contrast pass: `BoxStackPrototypePalette` keeps the same six palette families but increases background value/saturation contrast so the generated vertical gradient is clearly visible in Editor Play. Block tinting now moves farther through the palette and uses less white blending, while the generated floor uses stronger top/bottom contrast. `dotnet build BoxStack.slnx` passed with 0 warnings/errors, and Unity CLI Connector Editor Play confirmed marker `B031`, generated `16x256` background, generated `256x32` floor, strong bottom-to-top background brightness delta, and 0 console warnings/errors.
- B032 rear background gradient contrast pass: The change is deliberately limited to the rear background. `BoxStackPrototypePalette` now pushes background bottom colors close to dark, keeps the middle band saturated, and keeps the top band bright. `BoxStackPrototypeAssetLoader.CreateStackGradientBackgroundSprite` now places the middle transition at 46% height instead of 58%, so the bright upper color appears more clearly in the normal Game view. Block, floor, and UI palette behavior are otherwise left as-is from B031. `dotnet build BoxStack.slnx` passed with 0 warnings/errors, and Unity CLI Connector Editor Play confirmed marker `B032`, generated `16x256` background, near-dark bottom pixel, bright top pixel, strong bottom-to-top brightness delta, and 0 console warnings/errors.
- B033 Stack-like rear background direction pass: The rear background keeps the stronger contrast but flips the value direction to match the `Stack` reference better: bottom colors are now light/soft, middle bands stay saturated, and top colors are deep/dark. Block, floor, and UI palette behavior remain unchanged from the previous palette pass. `dotnet build BoxStack.slnx` passed with 0 warnings/errors, and Unity CLI Connector Editor Play confirmed marker `B033`, generated `16x256` background, bright bottom pixel, dark top pixel, strong bottom-to-top color distance, and 0 console warnings/errors.
- B034 rear background full-gradient fitting fix: Investigation showed palette edits were reaching runtime, but the `16x256` gradient sprite was uniformly scaled to cover the camera, causing Game view to sample only a narrow central band and read as a flat color. `FitSpriteToCoverWorldSize` now scales X and Y independently so the full vertical gradient fills the screen. `dotnet build BoxStack.slnx` passed with 0 warnings/errors, and Unity CLI Connector Editor Play confirmed marker `B034`, generated `16x256` background, full-gradient pixel colors, non-uniform background scale `(5.495, 0.611, 1.000)`, and 0 console warnings/errors.
- B035 Stage 1 palette restore: Restored the AquaBlue background hue values after the manual HSV experiment. Stage 1 now uses bottom hue `0.47`, middle hue `0.52`, and top hue `0.61` again, while keeping the B034 full-gradient fitting fix. `dotnet build BoxStack.slnx` passed with 0 warnings/errors, and Unity CLI Connector Editor Play confirmed marker `B035`, restored AquaBlue colors, non-uniform background scale `(5.495, 0.611, 1.000)`, and 0 console warnings/errors.
- B036 rear background contrast softening: Softened the Stack-like rear-background gradient across all stage palettes after confirming the previous high contrast was compensating for a now-fixed display bug. The background still keeps the light-bottom/dark-top direction, but top colors are raised from near-black to calmer dark values and saturation is slightly reduced. `dotnet build BoxStack.slnx` passed with 0 warnings/errors, and Unity CLI Connector Editor Play confirmed marker `B036`, softened AquaBlue colors, full-gradient background scale `(5.495, 0.611, 1.000)`, and 0 console warnings/errors.
- B037 flat progressive block colors: Simplified the generated Stack-like block sprite into a flat single-color rectangle mask and changed block tinting from ping-pong color movement to stage-target-based progression. Lower blocks start from a lightened `BlockBase`, higher blocks move toward a darkened `BlockAccent`, and the active falling block is only slightly brightened for readability. `dotnet build BoxStack.slnx` passed with 0 warnings/errors, and Unity CLI Connector Editor Play confirmed marker `B037`, a one-color generated block sprite, darker final block tint than first block tint, and 0 console warnings/errors.
- B038 flat block outline readability: Adds a darker outer edge to the generated flat block sprite because the fully single-color B037 blocks were too hard to read over the background. The block face remains tint-driven and flat; only the edge pixels are darker for silhouette readability. `dotnet build BoxStack.slnx` passed with 0 warnings/errors, and Unity CLI Connector Editor Play confirmed marker `B038`, a two-color generated block sprite with dark edge pixels, darker final block tint than first block tint, and 0 console warnings/errors.
- B039 remove block landing color shift: Removes the active-block brightening that made boxes appear to darken when they landed. `GetBlockTint` now returns the same progressive tint for moving and landed blocks, so placement does not change the box color. `dotnet build BoxStack.slnx` passed with 0 warnings/errors, and Unity CLI Connector Editor Play confirmed marker `B039`, matching active/placed tint values, darker final block tint than first block tint, and 0 console warnings/errors.
- B040 subtle block face gradient: Adds a subtle grayscale gradient to the generated Stack-like block face so it keeps the current tint progression but no longer reads as a fully flat app-style rectangle. The darker outer edge and no-landing-shift tint behavior remain unchanged. `dotnet build BoxStack.slnx` passed with 0 warnings/errors, and Unity CLI Connector Editor Play confirmed marker `B040`, generated `96x96` block texture, bottom/mid/top face values `0.890 / 0.941 / 0.988`, darker edge value `0.478`, and 0 console warnings/errors.
- B041 high-contrast block face gradient test: Increases the B040 generated block-face gradient contrast after the subtle pass was not visible enough in Game view. The darker outer edge and no-landing-shift tint behavior remain unchanged. `dotnet build BoxStack.slnx` passed with 0 warnings/errors, and Unity CLI Connector Editor Play confirmed marker `B041`, generated `96x96` block texture, bottom/mid/top face values `0.655 / 0.812 / 0.965`, darker edge value `0.478`, and 0 console warnings/errors.
- B042 block contact shadow pass: Keeps the accepted B041 block-face gradient and adds a generated `96x24` soft contact-shadow sprite under each Stack-like abstract block. `BoxStackPrototype` now attaches a `Contact Shadow` child renderer during block creation, uses palette-derived dark translucent tinting, and spaces block/shadow sorting orders so upper-block shadows can read over lower blocks. Gameplay physics, collision, stage tuning, movement, undo, and UI layout are unchanged. `dotnet build BoxStack.slnx` passed with 0 warnings/errors, and Unity CLI Connector Editor Play confirmed marker `B042`, one active-box contact shadow, shadow sprite `96x24`, and 0 console warnings/errors.
- B043 contact shadow visibility pass: Increases the B042 contact-shadow test values after the first pass was not visible enough by eye. The shadow now sits lower under the block, scales wider/taller, and uses stronger alpha (`0.72`) while keeping the accepted B041 block-face gradient unchanged. Gameplay physics, collision, stage tuning, movement, undo, and UI layout are unchanged. `dotnet build BoxStack.slnx` passed with 0 warnings/errors, and Unity CLI Connector Editor Play confirmed marker `B043`, one active-box contact shadow, shadow sprite `96x24`, shadow local position around `(0.00, -0.53, 0.00)`, shadow local scale around `(1.40, 3.12, 1.00)`, and 0 console warnings/errors.
- B044 contact shadow removal: Manual judgement found contact shadows made the block treatment feel stranger rather than better, so the contact-shadow sprite generation, child renderer creation, and shadow sorting changes were removed. The accepted B041 block-face gradient, darker outline, no-landing-shift tint behavior, gameplay physics, collision, stage tuning, movement, undo, and UI layout are unchanged. Next verification should confirm marker `B044` and no runtime `Contact Shadow` objects.
- B045 block face separation test was rejected: A non-shadow 2.5D top/right face separation was tested after B044, but manual judgement found it did not feel natural. The face-separation code was removed and the active runtime marker returned to `B044`; do not apply that approach unless the visual direction is reconsidered later.
- B046 central feedback toast removal: User judged the in-play `좋아요`/`유지!` style feedback UI cheap-looking, so `BoxStackPrototypeUi` no longer creates or refreshes the central feedback toast and `BoxStackPrototypeUiStateFactory` no longer emits HUD feedback copy. Result popups, stage select, undo, progress slots, gameplay, and block visuals are unchanged. Runtime marker is `B046`. `dotnet build BoxStack.slnx` passed with 0 warnings/errors.
- Stage plan: A 20-stage fixed-clear difficulty plan is recorded at `design/quick-specs/20-stage-difficulty-plan-2026-05-04.md` and now wired through `Assets/Resources/Prototype/BoxStackPrototypeConfig.asset`. Stage navigation is available during Editor Play with previous/next keyboard controls within the unlocked range, and tapping/clicking the HUD stage label opens a simple 20-stage select overlay where locked stages are disabled. After playtesting, stages 10+ feel significantly harder, so the prototype now uses a stage-limited HUD undo button instead of lowering the difficulty table. Main mode should use fixed box-count clears; infinite stacking is deferred as a later challenge-mode experiment.
- Unity editor control: Unity CLI Connector is installed and verified. See `docs/workflow/unity-cli-connector.md`; use it only when an AI agent must directly control or inspect the currently open Unity Editor to continue the task. Do not use it only for Play Mode validation; the user verifies actual gameplay feel, UI feel, difficulty, and touch/control feel directly in Unity Editor Play Mode.
- AIT/WebGL test notes: `docs/workflow/ait-webgl-testing-notes.md` now reflects the current UI Toolkit runtime layer, B022 asset-loading boundary, B023 visual catalog boundary, B027 build marker, game font resources, AIT Dev Server LAN URL, and the policy that AI agents should use `dotnet build BoxStack.slnx` for routine C# validation, skip Unity CLI Connector Play Mode checks when they are only for validation, leave actual Play Mode feel checks to the user, and reserve WebGL/phone builds for milestone browser/device spot checks.
- Commit checkpoint: WebGL resource loading, mobile safe-area UI, physics tuning, build marker, AIT/WebGL notes, AIT WebGL build settings, Korean WebGL font fallback, free-only rescue flow, floor/parcel friction tuning, softened falling-box impact, and screen-clamped movement speed correction were split into focused Korean commits through `54c57e3 박스 좌우 이동 속도 보정`. The worktree was clean immediately after that commit checkpoint.
- Playtest verdict: Basic game system and box-stacking feel are acceptable for prototype continuation. B013 phone testing confirmed that the separate HUD undo button fits later-stage recovery better than fail-popup continuation, and the change was committed as `12692f8 되돌리기 버튼 방식으로 변경`. B015 Editor Play checks confirm the UI Toolkit HUD, stage select, result popup, and overlay input blocking are viable for the next manual playtest.

## Next Action

B046 removes the in-play central feedback toast and keeps the accepted high-contrast B041 block face gradient plus darker outline. Contact shadows and the rejected B045 top/right face split remain intentionally excluded. Next check should be a user-led Editor Play feel pass confirming that normal play reads well with no central landing feedback. WebGL/mobile testing remains deferred to the next milestone spot check.

Routine validation policy: after code changes, use `dotnet build BoxStack.slnx` by default. Skip Unity CLI Connector Play Mode checks when they are only for validation; the user will perform real gameplay/UI feel checks directly in Unity Editor Play Mode.

## Open Questions

- Which abstract block visual style best fits BoxStack: clean flat blocks, lightly beveled 2D blocks, or stronger pseudo-3D blocks?
- How many box variations are needed before the stack stops feeling repetitive?
- Should the next playtest measure visual clarity, replay desire, or perceived brand fit?
- Should the background stay screen-fixed for prototype readability, or eventually scroll/parallax with stack height?
- What bottom padding feels best on the target portrait app-in-app viewport?
- Does the safe-area-aware UI Toolkit layout keep HUD and popups clear of Toss webview/status-bar insets on real devices?
- Is the duplicate `Assets/Resources/Prototype/...` copy acceptable through the prototype phase, or should it be replaced later with serialized/build-included asset references?
- At the next milestone WebGL checkpoint, does the phone browser load the latest AIT/WebGL build reliably through the PC LAN IP?
- At the next milestone WebGL checkpoint, does the WebGL build render Korean text correctly across HUD, stage select, result popup, and undo button states?
- Does the top-center box progress panel stay readable on the target phone viewport across 4-12 box stages?
- Do DNF BitBit v2 and Gmarket Sans Bold improve the game feel without hurting small-text readability on the target phone viewport?
- Should the next HUD playtest keep the active no-toast direction, or should feedback return later only when tied to a real placement-quality scoring signal?
- Is the HUD stage-label click plus stage select overlay enough for prototype stage testing?
- What non-shadow, non-face-split approach should improve block depth next without making the block look noisy or less Stack-like?
- Does screen-clamped centered constant-speed movement keep all box shapes visible while preserving harder-stage speed feel?
- Are the current single-column tolerance (`0.75`) and 5-second clear validation window fair enough across stages 1, 5, 10, 15, and 20?
- Does immediate collapse detection feel fair, or does it punish harmless physics wobble too quickly?
- Does the approved B008 falling-box impact tuning still feel right across stages 1, 5, 10, 15, and 20?
- Does the one-use HUD undo button make stages 10+ feel fair without fighting already-collapsing physics?
- Should the committed Delivery Arcade PNG mini pack remain as historical reference only now that the active direction is Stack-like 2D?
- Is PlayerPrefs enough for prototype progression testing before App-in-Toss storage requirements are confirmed?
- Are the Editor/development-only `진행 초기화` and `전체 해금` controls enough for fast stage testing, or should they move behind a less visible debug gesture later?

## Risks

- Progress dashboard can become stale if active session state changes without a matching dashboard update.
- AI-generated assets may look inconsistent unless the first prompt set tightly controls angle, outline, color, and export rules.
- Visual polish may hide physics readability issues if box silhouettes and contact edges are not kept clear.
- Background detail may reduce falling-box readability if the center play lane feels too busy on mobile.
- Camera floor clamp can make the starting view feel too high if the target portrait aspect ratio changes significantly.
- B017 removed the old fail-popup rescue and mock reward-ad runtime paths from `BoxStackPrototype.cs`. B018 migrates the config asset to the current `UndosPerStage` name while keeping `FormerlySerializedAs` for older serialized assets.
- The previous runtime-created UI Toolkit `PanelSettings` theme warning is fixed by loading the checked-in `BoxStackPanelSettings` asset. One unrelated Visual Studio UDP warning still appears in this local Editor setup.
- The prototype now includes multiple full Korean fonts in `Resources`, which improves visual direction but increases build weight until UI/font handling is replaced or subsetted.
- Safe area is handled in the prototype UI Toolkit layer, but real App-in-Toss WebGL/device testing is still needed because Editor Game view may not emulate every inset.
- AIT Dev Server now launches from the Unity menu after the PATH fix, but this depends on the AIT embedded pnpm folder remaining available on Windows `PATH`.
- WebGL sprite parity is confirmed in PC browser after the AIT/WebGL rebuild, but phone browser testing is still needed.
- Later 12-box stages may feel random if physics instability dominates player timing skill.
- Screen-clamped movement should keep boxes visible on narrow mobile viewports, but B012 phone testing still needs to confirm the compensated later-stage speed feels fair instead of frantic.
- Softened falling-box impact felt good enough in B008 phone testing, but B012 representative stage testing should confirm it still works after the movement feel change.
- HUD undo should feel like a timely correction tool, but the button may need repositioning if it competes with drop input or crowds the top HUD on small screens.
- Stack-like 2D should reduce the app-like UI read, but it may feel too close to a plain clone if color, block shape, and interaction feel do not develop a BoxStack-specific identity.
- The Delivery Arcade PNG mini pack remains committed as historical design reference, but it is not wired into the active B046 runtime UI.
- Reward-style rescue is currently disabled; reintroducing it too early could make failure feel monetized before the stage difficulty is accepted as fair.
- PlayerPrefs is fine for local prototype persistence, but App-in-Toss production storage requirements may require replacing it later.
- `BoxStackPrototypeConfig` now defines the stage/tuning boundary, and a checked-in `Resources` config asset exists for inspector tuning. PC WebGL smoke confirmed the rebuilt player loads and behaves with B014 values; B016 was accepted in Editor Play layout review, B017 and B018 were committed, and B019 removes the UI Toolkit PanelSettings warning from normal Editor Play validation.
- Prototype progression controls are useful for playtest speed, and are now hidden from non-development user builds; confirm this again before any App-in-Toss package test.
- Unity batchmode import could not run while the project was already open, but routine AI-side Play Mode verification is no longer required for normal iteration.
- Unity CLI Connector requires the Unity Editor to be open with this project loaded; if editor control is genuinely needed and port `8090` does not respond, scan `8091` through `8099`.
